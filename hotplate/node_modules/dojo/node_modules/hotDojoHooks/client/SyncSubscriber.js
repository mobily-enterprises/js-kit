define(['hotplate/hotDojoHooks/SyncTopic','dojo/Deferred'], function(SyncTopic, Deferred) {
  return {
    load: function (id, require, callback) {
      SyncTopic.incSubscriber(id.split('|')[0]);
      //Sync.incSubscriber(id);
      callback();
    }
  };
});

