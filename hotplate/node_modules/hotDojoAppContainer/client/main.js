
require(['dojo/topic', "hotplate/hotDojoAppContainer/AppContainer"  ], function( topic, AppContainer ){


  // This global variable will be used by hotDojoAuth in case the password
  // needs to be retyped
  // FIXME: Think of a neater way of doing this, this really is beyond terrible. The trouble is that 
  loginValue = vars.hotDojoAppContainer.login;

  // Create the "application" object, and places them in the right spot.
  appContainer = new AppContainer( {} , 'appContainer');


  // console.log("Starting up the container!");
  setTimeout(function() {
    appContainer.startup();
  }, 3000);

});

