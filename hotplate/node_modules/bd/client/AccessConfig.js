
define([
  "dojo/_base/declare",
  "dojo/_base/json",
  "dojo/store/Observable",
  "dojo/topic",
  "dojo/on",
  "dojo/dom",
  "dojo/dom-class",
  "dojo/when",

  "dijit/_WidgetBase",
  "dijit/_TemplatedMixin",
  "dijit/_WidgetsInTemplateMixin",
  "dijit/form/Form",
  'dijit/layout/TabContainer',
  'dijit/layout/ContentPane',
  "dijit/Destroyable",

  'dgrid/OnDemandGrid',
  'dgrid/OnDemandList',
  'dgrid/Keyboard',
  'dgrid/Selection',
  'dgrid/_StoreMixin',

  "hotplate/hotDojoSubmit/defaultSubmit",
  "hotplate/hotDojoLogger/logger",
  "hotplate/hotDojoStores/stores",

  "hotplate/hotDojoWidgets/AlertBar",
  "hotplate/hotDojoWidgets/_OverlayMixin",
  "hotplate/hotDojoWidgets/BusyButton",

   ], function(
     declare
     , json
     , Observable
     , topic
     , on
     , dom
     , domClass
     , when

     , _WidgetBase
     , _TemplatedMixin
     , _WidgetsInTemplateMixin
     , Form
     , TabContainer
     , ContentPane
     , Destroyable

     , OnDemandGrid
     , OnDemandList
     , Keyboard
     , Selection
     , _StoreMixin

     , ds
     , Logger
     , stores

     , AlertBar
     , _OverlayMixin
     , BusyButton
 ){


  var AccessConfigTabContainer = declare('hotplate/bd/AccessConfigTabContainer', [ ContentPane, _TemplatedMixin, _WidgetsInTemplateMixin ], {

    widgetsInTemplate: true,

    templateString: '' +
      '<div class="configSection" >' +
      '  <div data-dojo-attach-point="widget" data-dojo-type="dijit/layout/ContentPane">' +
      '    <div data-dojo-type="dijit/layout/TabContainer" data-dojo-props="tabPosition: \'top\'" data-dojo-attach-point="settingsTab">' +
      '      <div data-dojo-type="hotplate/bd/AccessConfigGeneral" data-dojo-props="title:\'General\'"></div>' + 
      '    </div>' +
      '  </div>' +
      '</div>',
  });



  declare('hotplate/bd/AccessConfigGeneral', [ ContentPane, _TemplatedMixin, _WidgetsInTemplateMixin, _OverlayMixin ], {

    templateString: '' +
      '<div>' +
      '  <form data-dojo-type="dijit/form/Form" data-dojo-attach-point="formWidget" method="POST"> ' +
      '    <div data-dojo-type="hotplate.hotDojoWidgets.AlertBar" data-dojo-attach-point="alertBarWidget"></div>' +

      '    <input type="hidden" data-dojo-type="dijit/form/TextBox" name="_id" />' +
      '    <input type="hidden" data-dojo-type="dijit/form/TextBox" name="__v" />' +

      '    <div class="box"><p class="boxTitle">User information</p>' +

      /*'      <div class="inputField" style="display:inline-block; margin-right:30px">' +
      '        <label for="${id}_LocalName">Your name</label>' +
      '        <input id="${id}_LocalName" data-dojo-type="dijit/form/ValidationTextBox" style="width:10em;" maxlength="5" name="localName" data-dojo-props="" />' + 
      '      </div>' +
      '      <div class="inputField" style="display:inline-block;">' +
      '        <label for="${id}_LocalLastName">Last Name</label>' +
      '        <input id="${id}_LocalLastName" data-dojo-type="dijit/form/ValidationTextBox" style="width:40em" maxlength="10" name="localLastName" data-dojo-props="" />' +
      '      </div>' +
      '    </div>' + */

    '      <div class="inputField">' +
    '        <label for="${id}_LocalEmail">Email</label>' +
    '        <input id="${id}_LocalEmail" data-dojo-type="dijit/form/ValidationTextBox" style="width:40em" maxlength="80" name="localEmail" data-dojo-props="validator:sharedFunctions.hotCommonValidators.email, required:true, invalidMessage:sharedFunctions.hotCommonValidators.email(false)"/>' +
    '      </div>' +


      '    <input class="formSubmit" type="submit" data-dojo-attach-point="buttonWidget" data-dojo-type="hotplate/hotDojoWidgets/BusyButton" label="Update" />' +
      '  </form>' +
      '</div>',


    // The store for records
    recordStore: null,


    postCreate: function(){
      this.inherited(arguments);

      var that = this;

      // Sets the record store for the widget
      this.recordStore = stores('accessConfig', { workspaceIdCall: vars['hotDojoAppContainer']['workspaceId'] } );

      // When the form's overlay is clicked, try and show the form again
      on(this, 'overlayClick', function(e){
        this.onShow();
      });

      topic.subscribe('configChange/access', function(u, message){
        if( message && message.remote ){
          that.set( 'overlayed', true );
          that.set( 'overlayClickable', true );
        }
      });


 
      // Submit form, trying to save values
      this.formWidget.onSubmit = ds.defaultSubmit(this.formWidget, this.buttonWidget, function(){

        // Set the values about to be saved
        var formValues = that.formWidget.get('value');

        // Try to save the values
        when( that.recordStore.put( formValues )) .then(
          ds.UIMsg( that.buttonWidget, that.alertBarWidget, "Configuration saved!" ),
          ds.UIErrorMsg( that.formWidget, that.buttonWidget, that.alertBarWidget )
        ).then( function( res ) {
           // ...
        });

      }); // this.formWidget.onSubmit
    }, // postCreate()


    onShow: function(){

      var that = this;

      // By default, this widget is overlayed
      this.set( 'overlayed', true );
      this.set( 'overlayClickable', false );

      when( this.recordStore.get( userId )).then(
        ds.UIMsg(),
        ds.UIErrorMsg()
      ).then(
        function(res){

          // OK things worked out: the overlay can go, values are assigned to form
          that.set( 'overlayed', false );
          that.formWidget.set( 'value', res ); // Thanks Karl Tiedt :D

        },
        function(err){
          that.set('overlayClickable', true );
        }
      );

    },



  }); // declare('hotplate/bd/SettingsGeneral'


 return AccessConfigTabContainer;
});


