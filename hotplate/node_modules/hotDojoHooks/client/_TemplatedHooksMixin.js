define([
  'dojo/_base/declare',
  'dojo/_base/lang',

  ], function(
    declare
  , lang

  , protocol


  ){
    return  declare(null, {

      templatedHooks: true,

      constructor: function(){

        console.log("AAAAAAAHHHHHHHHHHHHHHH");
        console.log(this);

        // this.templateString += "CAZZZZZZ";

        // Declaring object variable in constructor to make sure that
        // they are not class-wide (since they will be in the prototype)
        this.templatedHooks = true;
      },

    });
  }
);

