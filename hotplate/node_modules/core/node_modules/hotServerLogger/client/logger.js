
/*

;(function() {

  function logger(strig){
    console.log(string);
  }

  // Expose the class either via AMD or the global object
  if(typeof define === 'function' && define.amd) {
    define(function() {
      return logger;
    });
  }
  else {
    this.logger = logger;
  }
}(this));

*/
