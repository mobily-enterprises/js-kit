define([
  'hotplate/hotStores/dojo/JsonRest',
  'dojo/domReady!',
  ],function(
  JsonRest
  ){


  // Stores
  var stores = {};

  // If the global variable for the stores is defined...
  if(
    typeof( vars ) == 'object' &&
    typeof( vars.hotStores ) == 'object' &&
    typeof( vars.hotStores.stores ) == 'object'
  ){

    var definedStores = vars.hotStores.stores;

    // Create the store
    stores[i] = new JsonRest({
      target: definedStores.target,
			idProperty: definedStores.idProperty,
      sortParam: definedStores.sortParam,
    });
  }

  return stores;

});


