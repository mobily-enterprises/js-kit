define([
  ],function(
  ){

  var g = {};

  // The app's alertBar
  //
  g.logToConsole = true;

  // Global variable set to "true" if the network is found to be down
  //
  g.networkDown = false;

  // Global timeout
  g.timeout = 6000;

  return g;
});


