var dummy
, hotplate =  require('hotplate')
, path = require('path')
;

hotplate.hotEvents.on( 'pageElements', 'hotDojoGlobalAlertBar', function( done ){
  done( null, {
    jses: [ 'mainAlertBar.js' ],
  });
});

// Simply activate path to client files
hotplate.hotEvents.on( 'clientPaths', 'hotDojoGlobalAlertBar', function( done ){
  done( null, [ path.join(__dirname, '../client') ] );
})


