var dummy
, hotplate =  require('hotplate')
, path = require('path')
;

exports.hotHooks = hooks = {}

hooks.pageElements = function( done ){
  done( null, { moduleName: 'hotDojoGlobalAlertBar', result:{
    jses: [ 'mainAlertBar.js' ],
  } } );
}

// Simply activate path to client files
hooks.clientPaths = function( done ){
  done( null, { moduleName: 'hotDojoGlobalAlertBar', result: [ path.join(__dirname, '../client') ] } );
}


