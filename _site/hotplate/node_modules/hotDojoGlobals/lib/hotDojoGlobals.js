
var 
  hotplate =  require('hotplate')
, path = require('path')
;

var hooks = exports.hotHooks = {}

hooks.run = function( done ){
  done( null );
}

/*
hooks.pageElements = function( done ){
  done( null,  { moduleName: 'hotDojoStoreConfig', result:{ 
    csses: [ 'AlertBar.css', 'EditingWidget.css' ],
  } } );
}
*/

// Simply activate path to client files
hooks.clientPaths = function( done ){
  done( null, { moduleName: 'hotDojoGlobals', result: [ path.join(__dirname, '../client') ] } );
}


