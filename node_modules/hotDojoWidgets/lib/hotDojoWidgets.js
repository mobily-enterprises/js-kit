"use strict";

var dummy
  , hotplate =  require('hotplate')
  , path = require('path')
;


hotplate.hotEvents.on('pageElements', 'hotDojoWidgets', function( done ){
  done( null, { 
    csses: [ 'AlertBar.css', 'EditingWidget.css','_OverlayMixin.css' ],
  });
});

// Simply activate path to client files
hotplate.hotEvents.on( 'clientPath', 'hotDojoWidgets', function( done ){
  done( null, path.join(__dirname, '../client') );
})


/*
hotplate.hotEvents.on( 'pageElements', 'hotDojoWidgets', hotplate.cachable( function( done ){

  var imgPath = path.join( hotplate.config.get( 'hotplate.moduleFilesPrefix'), 'hotDojoWidgets', 'reload.png' );

  var headLines = [
    '<img src="' + imgPath + '"></img>'
  ];

  done( null, { headLines: headLines } );

}));
*/
