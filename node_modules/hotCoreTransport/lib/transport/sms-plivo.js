"use strict";

var dummy
  , path = require('path')
  , hotplate = require('hotplate')
  , declare = require('simpledeclare')
  , debug = require('debug')('hotplate:hotCoreTransport:plivo')
  , SimpleDbLayer = require( 'simpledblayer' )
  , SimpleSchema = require( 'simpleschema' )
  , JsonRestStores = require( 'jsonreststores' )

  , hotCoreJsonRestStores = require( 'hotCoreJsonRestStores' )
  , hotCoreStoreRegistry = require( 'hotCoreStoreRegistry' )
  , hotCoreServerLogger = require( 'hotCoreServerLogger' )
;


exports = module.exports = {

  setRoutes: function( stores, app, configArray, done ){
    debug("SETTING ROUTES FOR PLIVO");

    

    done( null );
  },

  run: function( stores, configArray, done ){
    debug("RUNNING  FOR PLIVO");
    done( null );
  },

  sendMessage: function( messageTo, messageConfig, done ){
    debug("LAYER SENDING: ", messageTo );
    done( null );
  },



};



