/*

  TODO:
  * Check that login box greys out when updated
*/

define([
   "dojo/_base/declare",
   "dojo/dom-style",
   "dojo/request",
   "dojo/cookie",

   "dijit/_WidgetBase",
   "dijit/_TemplatedMixin",
   "dijit/_WidgetsInTemplateMixin",
   "dijit/_OnDijitClickMixin",

   "hotplate/hotDojoWidgets/_OverlayMixin",
   "hotplate/hotDojoSubmit/defaultSubmit",
   "hotplate/hotDojoWidgets/BusyButton",
   "hotplate/hotDojoWidgets/TempDialog",
   "hotplate/hotDojoAuth2/ValidationUsername",

   "../buttons/_SignInRecoverRegisterButton",
   "../buttons/_ManagerButton",
   "../buttons/_ResumeButton",

   ], function(
     declare
     , domStyle
     , request
     , cookie

     , _WidgetBase
     , _TemplatedMixin
     , _WidgetsInTemplateMixin
     , _OnDijitClickMixin

     , _OverlayMixin
     , ds
     , BusyButton
     , TempDialog
     , ValidationUsername

     , _SignInRecoverRegisterButton
     , _ManagerButton
     , _ResumeButton

 ){

  var ret = {};




  ret.SignIn = declare( [ _SignInRecoverRegisterButton ], {
    strategyId: 'local',
   
    postCreate: function(){
      this.inherited( arguments );

      var self = this;

      

      var F = declare( [ _WidgetBase, _TemplatedMixin, _WidgetsInTemplateMixin ] , { 
          
        templateString: '' +
          '<div>\n' +
          '  <div>\n' +
          '    <form data-dojo-type="dijit/form/Form" data-dojo-attach-point="formWidget">\n' +
          '      <label for="${id}_login">Login</label>\n' +
          '      <input name="login" id="${id}_login" data-dojo-type="hotplate/hotDojoAuth2/ValidationUsername" data-dojo-attach-point="login" data-dojo-props="required: true, ajaxOkWhen:\'present\', ajaxInvalidMessage:\'Login name invalid!\'"></input>\n' +
          '      <label for="${id}_password">Password</label>\n' +
          '      <input type="password" name="password" id="${id}_password" data-dojo-attach-point="password" data-dojo-type="dijit/form/TextBox" ></input>\n' +
          '      <input class="form-submit" type="submit" data-dojo-attach-point="buttonWidget" data-dojo-type="hotplate/hotDojoWidgets/BusyButton", label="Go"></input>\n' +
          '    </form>\n' +
          '  </div>\n' +
          '</div>\n' +
          '',
      });


      self.button.on('click', function( e ){

        cookie( 'local-signin', 'ajax', { path: '/' } );

        var f = new F( ) ;
        var formDialog = new TempDialog();
        formDialog.addChild( f );
        formDialog.startup();

        f.formWidget.on( 'submit', ds.defaultSubmit( f.formWidget, f.buttonWidget, function(){
          
          f.buttonWidget.cancel();
          var data = f.formWidget.get('value');
          data.responseType = 'ajax';
          request.post('/auth/signin/local', { data: data } ).then(
            function( res ){
              try {
                res = JSON.parse( res );
              } catch( e ) {};

              // That's all
              formDialog.hide();
              var successURL = vars['hotDojoAuth2']['successURLs']['signin'];
              if( typeof( successURL) === 'undefined' ){
                throw("The Login widget must be used in a hotDojoAuth2 login form where vars['hotDojoAuth2']['successURL'] is set");
              } else {
                window.location = successURL;
              }
             
            },
            function( err ){
              self._displayError( err );
            }
          );

          return false;
        })); 

      });
    },


  });


  ret.Recover = declare( [ _SignInRecoverRegisterButton ], {
    strategyId: 'local',
    

  });

  ret.Register = declare( [ _SignInRecoverRegisterButton ], {
    strategyId: 'local',

    postCreate: function(){
      this.inherited( arguments );

      var self = this;

      var F = declare( [ _WidgetBase, _TemplatedMixin, _WidgetsInTemplateMixin ] , { 
          
        templateString: '' +
          '<div>\n' +
          '  <div>\n' +
          '    <form data-dojo-type="dijit/form/Form" data-dojo-attach-point="formWidget">\n' +
          '      <label for="${id}_login">Login</label>\n' +
          '      <input name="login" id="${id}_login" data-dojo-type="hotplate/hotDojoAuth2/ValidationUsername" data-dojo-attach-point="login" data-dojo-props="required: true, ajaxOkWhen:\'absent\', ajaxInvalidMessage:\'Login name taken!\'"></input>\n' +
          '      <label for="${id}_password">Password</label>\n' +
          '      <input type="password" name="password" id="${id}_password" data-dojo-attach-point="password" data-dojo-type="dijit/form/TextBox" ></input>\n' +
          '      <label for="${id}_passwordConfirm">Confirm password</label>\n' +
          '      <input type="password" name="passwordConfirm" id="${id}_passwordConfirm" data-dojo-attach-point="passwordConfirm" data-dojo-type="dijit/form/ValidationTextBox" data-dojo-props="validator: this.passwordMatch.bind( this ), invalidMessage:\'Passwords must match\'" ></input>\n' +
          '      <input class="form-submit" type="submit" data-dojo-attach-point="buttonWidget" data-dojo-type="hotplate/hotDojoWidgets/BusyButton", label="Go"></input>\n' +
          '    </form>\n' +
          '  </div>\n' +
          '</div>\n' +
          '',

        passwordMatch: function( p ){
          return p === this.password.value;
        },
      });


      self.button.on('click', function( e ){

        cookie( 'local-register', 'ajax', { path: '/' } );

        var f = new F( ) ;
        var formDialog = new TempDialog();
        formDialog.addChild( f );
        formDialog.startup();

        f.formWidget.on( 'submit', ds.defaultSubmit( f.formWidget, f.buttonWidget, function(){
          
          f.buttonWidget.cancel();
          var data = f.formWidget.get('value');
          data.responseType = 'ajax';
          request.post('/auth/register/local', { data: data } ).then(
            function( res ){
              try {
                res = JSON.parse( res );
              } catch( e ) {};

              // That's all
              formDialog.hide();
              var successURL = vars['hotDojoAuth2']['successURLs']['register'];

              if( typeof( successURL) === 'undefined' ){
                throw("The Login widget must be used in a hotDojoAuth2 login form where vars['hotDojoAuth2']['successURL'] is set");
              } else {
                window.location = successURL;
              }
             
            },
            function( err ){
              self._displayError( err );
            }
          );

          return false;
        })); 

      });
    },








  });

  ret.Resume = declare( [ _ResumeButton ], {
    strategyId: 'local',
   
    postCreate: function(){
      this.inherited( arguments );

      var self = this;

      var F = declare( [ _WidgetBase, _TemplatedMixin, _WidgetsInTemplateMixin ] , { 
          
        templateString: '' +
          '<div>\n' +
          '  <div>\n' +
          '    <form data-dojo-type="dijit/form/Form" data-dojo-attach-point="formWidget">\n' +
          '      <label for="${id}_password">Password</label>\n' +
          '      <input type="password" name="password" id="${id}_password" data-dojo-attach-point="password" data-dojo-type="dijit/form/TextBox" ></input>\n' +
          '      <input class="form-submit" type="submit" data-dojo-attach-point="buttonWidget" data-dojo-type="hotplate/hotDojoWidgets/BusyButton", label="Go"></input>\n' +
          '    </form>\n' +
          '  </div>\n' +
          '</div>\n' +
          '',
      });


      self.button.on('click', function( e ){

        cookie( 'local-resume', 'ajax', { path: '/' } );

        var f = new F( ) ;
        var formDialog = new TempDialog();
        formDialog.addChild( f );
        formDialog.startup();

        f.formWidget.on( 'submit', ds.defaultSubmit( f.formWidget, f.buttonWidget, function(){
          
          f.buttonWidget.cancel();
          var data = f.formWidget.get('value');
          data.responseType = 'ajax';
          
          request.post('/auth/resume/local', { data: data } ).then(
            function( res ){
              try {
                res = JSON.parse( res );
              } catch( e ) {};

              // That's all
              formDialog.hide();
              console.log("RESUME SUCCESSFUL!!!");
            },
            function( err ){
              self._displayError( err );
            }
          );

          return false;
        })); 

      });
    },


  });




  ret.Manager = declare( [ _ManagerButton ], {

    strategyId: 'local',

    postCreate: function(){
      this.inherited( arguments );

      var self = this;

      var F = declare( [ _WidgetBase, _TemplatedMixin, _WidgetsInTemplateMixin ] , { 
          
        templateString: '' +
          '<div>\n' +
          '  <div>\n' +
          '    <form data-dojo-type="dijit/form/Form" data-dojo-attach-point="formWidget">\n' +
          '      <label for="${id}_login">Login</label>\n' +
          '      <input name="login" id="${id}_login" value="${login}" data-dojo-type="hotplate/hotDojoAuth2/ValidationUsername" data-dojo-attach-point="login", data-dojo-props="alwaysOk: \'${login}\', required: true, ajaxOkWhen:\'absent\', ajaxInvalidMessage:\'Login name taken!\'" ></input>\n' +
          '      <label for="${id}_password">Password</label>\n' +
          '      <input type="password" name="password" id="${id}_password" value="${password}" data-dojo-attach-point="password" data-dojo-type="dijit/form/TextBox"></input>\n' +
          '      <input class=form-submit type="submit" data-dojo-attach-point="buttonWidget" data-dojo-type="hotplate/hotDojoWidgets/BusyButton" label="Go"></input>\n' +
          '    </form>\n' +
          '  </div>\n' +
          '  <button data-dojo-attach-point="deleteButtonWidget" data-dojo-type="hotplate/hotDojoWidgets/BusyButton" label="Delete" />\n' +
          '</div>\n' +
          '',
      });


      self.button.on('click', function( e ){

        cookie( 'local-manager', 'ajax', { path: '/' } );

        // Make up the constructor parameters for the form
        var cp = { login: '', password: '' };
        if( self.strategyData ) cp = { login: self.strategyData.field1.toLowerCase(), password: self.strategyData.field2 };

        var f = new F( cp ) ;
        if( ! self.strategyData ){
          domStyle.set( f.deleteButtonWidget.domNode, 'display', 'none' );
        }
        var formDialog = new TempDialog();
        formDialog.addChild( f );
        formDialog.startup();

        f.formWidget.on( 'submit', ds.defaultSubmit( f.formWidget, f.buttonWidget, function(){
        //f.formWidget.on( 'submit', function( e ){
          
          f.buttonWidget.cancel();
          var data = f.formWidget.get('value');
          data.responseType = 'ajax';
          request.post('/auth/manager/local', { data: data } ).then(
            function( res ){
              try {
                res = JSON.parse( res );
              } catch( e ) {};

              // Pre-emptive changes to self.strategydata, so that the data is editable
              // straight away
              self.strategyData = {};
              self.strategyData.field1 = f.formWidget.get('value').login.toLowerCase();
              self.strategyData.field2 = f.formWidget.get('value').password;
              self.strategyData[ self.store.idProperty ] = res.user[ 'userStrategiesId' ];

              // Update what's "always OK" for the validator call. Not strictly
              // necessary as the form will close.
              f.login.alwaysOk = f.formWidget.get('value').username;

              // That's all

              formDialog.hide();
            },
            function( err ){
              self._displayError( err );
            }
          );

          return false;
        })); 
        //}); 


        f.deleteButtonWidget.on( 'click', function( e ){
          self._deleteStrategyData( "Delete credentials?", "Are you sure you want to invalidate your login/password credentials?" ).then(
            function( res ){
              formDialog.hide();
            }
          );
        });


      });
    },








  });

  return ret;
});





