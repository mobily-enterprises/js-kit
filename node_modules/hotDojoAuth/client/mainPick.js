define( [ "dojo/topic", "dojo/parser", "hotplate/hotDojoAuth/Pick" ], function( topic, parser, Pick ){

  topic.subscribe( 'hotplateModulesLoaded', function(){

    var w = new Pick({}, 'pick' ); 
    w.startup();
  });
});
