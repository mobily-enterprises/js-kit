define( [ "dojo/_base/declare" ], function( declare ){

  return declare( null, {

    constructor: function(){

      // Compile the template string. Doing this at constructor time, as
      // it needs to happen at the earliest possible moment
      //eval( this.jadeTemplate );
      //if( typeof( anonymous ) === 'function' ){
      //  this.templateString = anonymous();
      //} else {
      //  throw("Invalid Jade-compiled function");
      //}

      // Compile the template string. Doing this at constructor time, as
      // it needs to happen at the earliest possible moment
      this.templateString = jade.compile( this.templateString )();

    },

  });
});
